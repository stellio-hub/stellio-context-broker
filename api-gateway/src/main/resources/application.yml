spring:
  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/ngsi-ld/v1/**]':
                allowedOrigins: ${APPLICATION_CORS_ALLOWED_ORIGINS:http://localhost:3000}
                allowedMethods:
                  - GET
                  - POST
                  - PATCH
                  - PUT
                  - DELETE
                  - HEAD
                  - OPTIONS
                allowedHeaders: "*"

application:
  search-service:
    url: http://localhost:8083
  subscription-service:
    url: http://localhost:8084

management:
  endpoints:
    web:
      exposure:
        include: health,info
    access:
      default: none
  endpoint:
    info:
      access: none
    health:
      access: read_only
  # URLs to the OpenTelemetry collector
  # Log data are sent only if the 'otel' Spring profile is active
  opentelemetry:
    logging:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/logs
    # https://opentelemetry.io/docs/specs/semconv/registry/attributes/service/
    # https://opentelemetry.io/docs/specs/semconv/registry/attributes/deployment/
    resource-attributes:
      service.name: api-gateway
      service.namespace: stellio
      deployment.environment.name: local
  otlp:
    metrics:
      export:
        url: http://localhost:4318/v1/metrics
        enabled: false

logbook:
  format.style: http
  strategy: body-only-if-status-at-least
  minimum-status: 200
  predicate:
    include:
      - path: /ngsi-ld/v1/**
