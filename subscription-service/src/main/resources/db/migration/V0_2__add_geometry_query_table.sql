CREATE TABLE geometry_query(
    georel          VARCHAR(255) NOT NULL,
    geometry        VARCHAR(255) NOT NULL,
    coordinates     VARCHAR(255) NOT NULL,
    geoproperty     VARCHAR(64),
    subscription_id VARCHAR(64) NOT NULL
);
